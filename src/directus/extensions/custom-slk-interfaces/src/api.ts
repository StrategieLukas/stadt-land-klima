// API entry point for Directus bundle extension
// Must export configs object with hooks, endpoints, operations arrays

const configs = {
  hooks: [],
  endpoints: [],
  operations: [],
};

export { configs };
