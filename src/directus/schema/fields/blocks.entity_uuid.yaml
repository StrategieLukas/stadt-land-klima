collection: blocks
field: entity_uuid
type: string
meta:
  collection: blocks
  field: entity_uuid
  interface: input
  note: Slug or identifier of parent entity
  required: true
  sort: 6
  translations:
    - language: de-DE
      translation: Entitäts-UUID
  width: half
schema:
  name: entity_uuid
  table: blocks
  data_type: character varying
  max_length: 255
  is_nullable: false
  is_indexed: true
