collection: user_registrations
field: requires_admin_approval
type: boolean
meta:
  collection: user_registrations
  display: boolean
  field: requires_admin_approval
  hidden: false
  interface: boolean
  note: "True if registering for a new local team (no existing team)"
  required: false
  sort: 9
  width: half
  translations:
    - language: de-DE
      translation: Admin-Genehmigung erforderlich
schema:
  name: requires_admin_approval
  table: user_registrations
  data_type: boolean
  default_value: false
