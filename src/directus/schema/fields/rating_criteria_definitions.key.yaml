collection: rating_criteria_definitions
field: key
type: string
meta:
  collection: rating_criteria_definitions
  conditions: null
  display: null
  display_options: null
  field: key
  group: null
  hidden: false
  interface: input
  note: "Unique key per measure, snake_case (e.g., has_climate_action_plan)"
  options:
    slug: true
    placeholder: "has_climate_action_plan"
  readonly: false
  required: true
  sort: 3
  special: null
  translations:
    - language: de-DE
      translation: Schlüssel
  validation:
    _and:
      - key:
          _regex: "^[a-z][a-z0-9_]*$"
  validation_message: "Must be snake_case (lowercase letters, numbers, underscores)"
  width: half
schema:
  name: key
  table: rating_criteria_definitions
  data_type: varchar
  default_value: null
  max_length: 64
  numeric_precision: null
  numeric_scale: null
  is_nullable: false
  is_unique: false
  is_primary_key: false
