// Registers the <altcha-widget> web component on the client side.
// Importing the package is enough — it self-registers the custom element.
import 'altcha';

export default defineNuxtPlugin(() => {});
