// Import intersection observer polyfill for browser compatibility
import 'intersection-observer'

export default defineNuxtPlugin(() => {
  // Plugin is loaded, intersection observer polyfill is available
})